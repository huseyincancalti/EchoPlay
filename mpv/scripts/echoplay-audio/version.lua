-- Overwritten with the actual release tag (e.g. "1.4.0") by release.yml's "Embed version into
-- script" step on every platform, right before the mpv/ config folder gets copied into the
-- build output. This placeholder is only ever seen when running from a raw git checkout.
return "0.0.0-dev"
